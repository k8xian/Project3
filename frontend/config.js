export const Config = {apiURL: "http://localhost/8080"};
// eventually this config file will end up pointing to the deployed backend
// this will get added to the gitignore after that and you will change this back to your local backend